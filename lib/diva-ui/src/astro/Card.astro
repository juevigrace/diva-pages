---
export interface Props {
  class?: string;
}

const { class: className, ...attrs } = Astro.props;
const cls = ['bg-card text-card-foreground rounded-xl border shadow', className].filter(Boolean).join(' ');
---

<div class={cls} {...attrs}>
  <slot />
</div>
